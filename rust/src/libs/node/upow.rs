pub struct UPOW {}

impl UPOW {
    pub fn compute_for(
        epoch: u64,
        trainer_sk: &Vec<u8>,
        pop: &Vec<u8>,
        pk: &Vec<u8>,
        segment_vr: &[u8],
        itrs: i32,
    ) {
    }

    pub fn branch_sol(
        epoch: u64,
        trainer_sk: &Vec<u8>,
        pop: &Vec<u8>,
        pk: &Vec<u8>,
        segment_vr: &[u8],
        itrs: i32,
    ) {
        
    }
}
